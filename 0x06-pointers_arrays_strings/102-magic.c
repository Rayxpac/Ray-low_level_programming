#include <stdio.h>
/**
 * enty point
 *
 * you are not allow to use a
 * you ara not allow to modfy p
 */
int main(void)
{
int n;
int a[5];
int *p;
a[2] = 1024;
p = &n;
printf("a[2] = %d\n", a[2]);
return (0);
}
